# 4-digit PIN required to enter the site at runtime.
# Set this in Netlify: Site settings -> Environment variables -> add VITE_PIN_GATE_PIN.
# Leave blank locally to bypass the gate during development.
VITE_PIN_GATE_PIN=
