fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic[email]==2.11.4
openai==1.78.1
sentry-sdk[fastapi]==2.22.0
python-jose[cryptography]==3.3.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.40
twilio==9.4.6
python-multipart==0.0.22
slowapi==0.1.9
python-dotenv==1.0.1
httpx==0.28.1
jinja2==3.1.6

# ── Geospatial (PostGIS) ─────────────────────────────────────────────────────
# GeoAlchemy2 adds native PostGIS GEOMETRY column types and spatial index
# support to SQLAlchemy.  Required in production with PostgreSQL + PostGIS.
# Install separately when running with PostgreSQL:
#   pip install geoalchemy2==0.15.2
# geoalchemy2==0.15.2   # uncomment for production PostGIS support

# ── Background tasks: Celery + Redis ────────────────────────────────────────
celery[redis]==5.4.0
redis==5.2.1

# ── LangChain RAG pipeline ───────────────────────────────────────────────────
langchain==0.3.27
langchain-openai==0.3.16
langchain-community==0.3.27
chromadb==0.6.3

# ── Production server ────────────────────────────────────────────────────────
gunicorn==23.0.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.26.0
